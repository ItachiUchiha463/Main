#include "Kovalchuk.h"
int main() {
	setlocale(0, "UKR");
	int choice=1;
	while (choice != 10) {
		cout << "Виберiть операцію" << endl;
		cout << "1-записати у файл" << endl;
		cout << "2-дозаписати у файл" << endl;
		cout << "3-прочитати файл" << endl;
		cout << "4-прочитати файл" << endl;
		cout << "5-сортування за ростом" << endl;
		cout << "6-однакова вага i однаковий номер взуття " << endl;
		cout << "7-номер одежi>46 i номер взуття" << endl;
		cout << "8-люди ріст яких в межах 10% та вага в межах 5%" << endl;
		cout << "9-людина з найменшим ростом i найменшим номером одежi та людина з найбiльшим номером взуття i найбільшою вагою" << endl;
		cout << "10-завершення роботи" << endl;
		cin >> choice;
		switch (choice) {
		case 1: { break; }
		case 2: { break; }
		case 3: { break; }
		case 4: { break; }
		case 5: { break; }
		case 6: { break; }
		case 7: { break; }
		case 8: { break; }
		case 9: { break; }

		}
	}
}